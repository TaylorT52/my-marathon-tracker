{
  "name": "runalong-firestore-tests",
  "private": true,
  "scripts": {
    "test": "mocha --timeout 10000 test.js"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0",
    "mocha": "^11.0.0"
  }
}
